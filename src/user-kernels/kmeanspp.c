#include <stdio.h>
#include <stdlib.h>
#include <omp.h>
#include <time.h>

void execute()
{
  int k = 0;
  int dim = 0;
  int n_points = 0;

  for (int i = 1; i < n_points; i++)
  {
    min_distances = EUCLEDIAN DISTANCE(centroidids, points);
    prob = min_distances / min_distances.sum()


  }

}
