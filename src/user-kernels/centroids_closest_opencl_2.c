#pragma OPENCL EXTENSION cl_khr_fp64 : enable


kernel void execute(global double *dist, global long *res, global double *min_dist)
{
  int i = get_global_id(0);
  ind j = get_global_id(1);

  if (j )

}
